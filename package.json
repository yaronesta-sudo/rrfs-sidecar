{
  "name": "rrfs-sidecar",
  "version": "1.0.0",
  "description": "Tiny sidecar that extracts 2m temperature from NOAA RRFS GRIB2 via wgrib2 and returns JSON. Used by weather-watchdog to feed the RRFS shadow model.",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
